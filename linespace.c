#include <stdio.h>
#include<conio.h>
void main()
{
FILE *fp;
char a[200];
char ch;
int linespace;
int count = 0;
printf("enter the char");
scanf("%c",&a);
for(ch==0;ch<0;ch++)
++linespace;
++ count;
{
printf("Lines : %d \n", linespace);
}
getch();
}
