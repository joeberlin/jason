#include <stdio.h>
#include<conio.h>
void main()
{
FILE *fp;
char a[200];
char ch;
int linecount;
linecount = 0;
printf("enter the char");
scanf("%c",&a);
for(ch==0;ch<0;ch++)
++linecount;
{
printf("Lines : %d \n", linecount);
}
getch();
}
